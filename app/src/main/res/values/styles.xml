<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Question background style. -->
    <style name="AppTheme.qStyle">
        <!--<item name="android:layout_marginBottom">6dp</item> -->
        <item name="android:paddingBottom">8dp</item>
        <item name="android:background">@color/colorQuestionBackground</item>
    </style>

    <!-- Card style -->
    <style name="cardStyle" parent="CardView">
        <item name="android:layout_margin">4dp</item>
        <item name="cardCornerRadius">6dp</item>
        <item name="cardElevation">4dp</item>
    </style>

    <!-- Question text style. -->
    <style name="AppTheme.qTextStyle" >
        <item name="android:textSize">17sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_margin">8dp</item>
    </style>

    <!-- Option style -->
    <style name="AppTheme.optionStyle" >
        <item name="android:textSize">16sp</item>
    </style>

    <!-- Button Style -->
    <style name="AppTheme.buttonStyle">
        <item name="android:textStyle">bold</item>
        <item name="android:backgroundTint">@color/colorAccent</item>
    </style>
</resources>